--- TERM.c	2025-01-05 19:18:49.470751619 +0100
+++ /home/nicolas/Term.c	2025-01-05 19:19:04.310124715 +0100
@@ -90,7 +90,6 @@
     return -1;
   else if (term.child == 0) {
     setenv("PS1", "\\[\\033[32m\\]\\w\\[\\033[00m\\]\\$ ", 1);
-    updateLibPath("/mnt/SDCARD/Apps/Terminal/lib");
 
     int slave_fd = open(slave_name, O_RDWR);
     struct termios terminal_attr;
@@ -105,16 +104,9 @@
       exit(EXIT_FAILURE);
     }
     close(slave_fd);
-    char* args[64];
-    args[0] = "/mnt/SDCARD/Apps/Terminal/screen";
-    args[1] = "-c";
-    args[2] = "/mnt/SDCARD/Apps/Terminal/.screenrc";
-    int i = 0;
-    while (cfg.args && cfg.args[i]) {
-      args[i + 3] = cfg.args[i];
-      i++;
-    }
-    args[i + 3] = NULL;
+    char *env_shell = getenv("SHELL");
+    if (env_shell == NULL) env_shell = "/bin/sh";
+    char **args = cfg.args ? cfg.args : (char *[]){env_shell, "-i", NULL};
 
     execvp(args[0], args);
     perror("execvp failed");
